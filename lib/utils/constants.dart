// lib/utils/constants.dart

import 'package:flutter/material.dart';

// General Constants
const String appTitle = 'AI Tutorials Platform';
const String baseApiUrl = 'https://api.example.com/';

// UI Constants
const Color primaryColor = Colors.blue;
const Color secondaryColor = Colors.white;
const double defaultPadding = 16.0;
const double defaultMargin = 16.0;

// Text Styles
const TextStyle headingStyle = TextStyle(
  fontSize: 24,
  fontWeight: FontWeight.bold,
);

const TextStyle subHeadingStyle = TextStyle(
  fontSize: 18,
  fontWeight: FontWeight.w600,
);

const TextStyle bodyStyle = TextStyle(
  fontSize: 14,
  color: Colors.black54,
);

// Error Messages
const String networkError = 'Failed to connect to the network. Please try again.';
const String serverError = 'Server error. Please try again later.';
